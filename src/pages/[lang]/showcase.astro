---
import ShowcasePage from "../../components/pages/ShowcasePage.astro";

export function getStaticPaths() {
  return [
    { params: { lang: 'en' } },
    { params: { lang: 'es' } },
  ];
}

const { lang } = Astro.params;
---

<ShowcasePage lang={lang} />
